class Business {
    constructor(type, address) {
        this.type = type
        this.address = address
    }

    getType() {
        return this.type
    }

    getAddress() {
        return this.address
    }
}

module.export = Business